# References

This directory bundles two independent skills:

- `kazix/` for the 卡兹克公众号长文 writing style
- `latepost/` for the 晚点 long-form routing and prompt templates

Each skill keeps its support files under its own `references/` subdirectory.
