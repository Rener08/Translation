# Archived Utilities

These scripts are kept out of the active project root so the main workflow stays clean.

## Moved Here

- `make_cookies_txt.py` - one-off cookie export helper for yt-dlp testing
- `test_proxy.py` - local proxy port probe
- `test_timing.py` - end-to-end timing probe for YouTube download + Whisper flow
- `check_youtube_downloadable.py` - batch yt-dlp downloadability checker
- `test_youtube_downloaders.py` - multi-backend downloader comparison probe
- `clean_chatgpt_export.py` - export cleanup helper for ChatGPT transcript JSON

These scripts are not part of the current product loop. If one becomes part of the workflow again, move it back into a documented active location and update the root docs.

